# Ignore everything then restore any directories or files with extensions, so binaries are properly excluded
*
!*.*
!*/
!/COPYING
!/Gemfile
!Dockerfile*

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Environment files, containing secrets
.env
*.p8
.vscode/
*.prettierrc*

# System files
Thumbs.db
.DS_Store
*.swp

# Documentation
docs/_site
docs/bin
docs/tmp

# GoReleaser
dist/
